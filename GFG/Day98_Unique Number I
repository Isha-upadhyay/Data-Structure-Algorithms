class Solution {
  public:
    int findUnique(vector<int> &arr) {
        // code here
        int n = arr.size();
        
        int total = arr[0];
        
        for(int i=1;i<n;i++){
            total ^= arr[i];
        }
        return total;
    }
};
